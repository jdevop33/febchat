{
  "version": 2,
  "buildCommand": "chmod +x ./build-with-db.sh && ./build-with-db.sh",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "env": {
    "NEXTAUTH_URL": {
      "type": "url",
      "description": "The base URL of your site for authentication",
      "default": "https://juche.org"
    }
  }
}